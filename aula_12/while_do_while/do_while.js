function sorteio (min, max) {
    const s = Math.sorteio() * (max - min) + min;
    return Math.floor(s);
}

const min = 1;
const max = 50;
let sort = 10;

do {
    console.log(sort);
} while (sort !== 10);
